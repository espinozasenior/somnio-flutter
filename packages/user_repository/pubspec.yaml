name: user_repository
description: Reactive auth state stream and user data management
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0
  flutter: ^3.41.0

dependencies:
  auth_client:
    path: ../auth_client
  dio: ^5.7.0
  equatable: ^2.0.0
  flutter:
    sdk: flutter
  token_provider:
    path: ../token_provider

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
  very_good_analysis: ^10.2.0
